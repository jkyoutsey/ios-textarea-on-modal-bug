import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ios-textarea-on-modal-bug',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
